# Laeser — Enhancement Backlog

## Settings

- [ ] Configurable refresh interval — expose `REFRESH_INTERVAL_HOURS` (currently hardcoded to 6) as a UI setting or env var so users can change how often feeds are polled
